																				Random codes 

> if we want to access the last code then simply type -1 will you get the list when you don't  actually know the size of the array. 

> APPENDING is adding in the order where as if you want to add at any specific place then use INSERT. 

> same for pop and removing. 

> for sorting there is a parameter called reverse which is in default false if you set it up 
in false then it will print in reverse mode. 

> there is a difference between sort and sorted where sort returns the array but sorted returns a new array and does not modify the original array. 
















												               LIST Codes 

# taking the list input in a new way:

try:
	n = int(input("enter the size of the list"))
	list1= list(map("int,input("enter the elements of the list").split()))
	
	if len(list1)==n:
		print("you have successfully created a list")
	else:
		print("error with the size of the array")

except Exception as e:
	print(f"the following error is what you got {e}")

	not le this should be inside a function to be called and it's good for competitive programming)

